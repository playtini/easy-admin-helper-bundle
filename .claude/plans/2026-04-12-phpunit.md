# PHPUnit upgrade & test coverage improvements (2026-04-12)

## PHPUnit 11 → 13 upgrade
- `composer.json`: `phpunit/phpunit` `^11.5` → `^13.0`, platform PHP `8.4` → `8.4.1` (PHPUnit 13 requires `>=8.4.1`)
- `phpunit.xml.dist`: schema URL updated to `13.0`
- `.github/workflows/build.yaml` and `pr.check.yaml`: PHPUnit version `11.5` → `13`
- No test code changes needed — existing tests were already compatible

## Test coverage: AdminFormatter (was 12% methods → 100%)
- Added tests for `breakWords`, `muteZero`, `formatExpireDate`, `formatBoolNullEmoji`, `formatHttpStatus`, `percents`
- Added edge case test for `formatUrlPath` with root URLs

## Test coverage: Entity traits (new)
- `ArchivableEntityTraitTest` — archive/unarchive, idempotency, IsEnabled interaction
- `NameEntityTraitTest` — get/set, null handling, 255-char truncation
- `NameUniqueEntityTraitTest` — same coverage for the unique variant
- `CreatableEntityTraitTest` — default value, set with date, set with null
- `IsEnabledTraitTest` — default, set, null, disabled-when-archived
- `CommentEntityTraitTest` — both `CommentEntityTrait` and `ShortCommentEntityTrait` (1024-char truncation)
- `UidEntityTraitTest` — get/set, auto-generation on null, 32-char truncation
- `VirtualFieldsEntityTraitTest` — all 9 virtual methods return expected defaults

## Smoke test: ClassLoadableTest (new)
- Auto-discovers all PHP files in `src/` and verifies each class/interface/trait is reflectable
- Catches syntax errors, missing imports, broken inheritance
- Covers all 42 classes — future-proof, no maintenance needed when adding new classes

## Results
- 18 → 100 tests, 54 → 175 assertions
- Line coverage: 4% → 17%
- Method coverage: 7% → 32%
- All testable classes at 100% coverage
